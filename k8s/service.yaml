apiVersion: v1
kind: Service
metadata:
  name: animals-api
  namespace: default
  labels:
    app: animals-api
spec:
  type: LoadBalancer
  selector:
    app: animals-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
    name: http
